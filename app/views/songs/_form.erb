<%= form_for @song do |f| %>
  <p>
  <%= f.label :name %>:
  <%= f.text_field :name %>
  </p>
  <p>
  <%= f.label :artist_id %>:
  <%= f.text_field :artist_id %>
  <%# f.collection_select :artist_id, Artist.all, :id, :name, :multiple => true %>

  </p>
  <p>
  <%= f.label :genre_id %>:
  <%= f.text_field :genre_id %>
  <%# f.collection_select :genre_id, Genre.all, :id, :name, :multiple => true %>

  </p>
  <%= f.submit %>
<% end %>
